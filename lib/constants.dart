import 'package:flutter/material.dart';

class TerminView extends StatelessWidget {
  const TerminView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
